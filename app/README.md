# Next.js App Router

Next.js **16** App Router (не 15). Team Overview = `app/page.tsx` (`/`), не обязателен route group `(dashboard)`. Карточка: `athletes/[id]` с async `params`. `api/health`.

Шаблон create-next-app подставляет Geist_Mono — заменить на JetBrains_Mono.

Не класть сюда чтение `data/synthetic` — только `WearableAdapter`.
