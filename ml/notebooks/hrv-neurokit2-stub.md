# NeuroKit2 HRV pipeline (stub)

Adapted from: https://github.com/neuropsychology/NeuroKit  
License: check upstream (typically MIT-compatible; confirm before commercial use).

Назначение: расчёт HRV (time-domain, frequency-domain) из RR-интервалов, валидация против Kubios — **позже**.

Сейчас: не подключать Python-сервис к Next.js API route. Вход MVP — дневной `hrvRmssd` из адаптера, не сырой ECG.
