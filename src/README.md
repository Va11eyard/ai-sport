# UI-библиотека

`components/` — gauges, bento, sparkline, heatmap, filters.  
`lib/wearables/` — интерфейс адаптера.  
`lib/scores/` — эвристики readiness/recovery/risk.  
`lib/athletes/` — состав и позиции.  
`styles/` — токены и grain overlay.
